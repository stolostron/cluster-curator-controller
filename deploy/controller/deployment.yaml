apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    name: cluster-curator-controller
  name: cluster-curator-controller
spec:
  replicas: 2
  selector:
    matchLabels:
      name: cluster-curator-controller
  template:
    metadata:
      labels:
        name: cluster-curator-controller
    spec:
      containers:
      - command:
        - ./cluster-curator-controller
        - "-v"
        - "2"
        image: registry.ci.openshift.org/open-cluster-management/cluster-curator-controller:latest        - name: POD_NAME
        env:
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        image: quay.io/jpacker/cluster-curator-controller@sha256:90419fd30a086ab0519112bd764e6c772575df88435580b188960cc206b38442
        imagePullPolicy: Always
        name: cluster-curator-controller
        resources:
          limits:
            cpu: "10m"
            memory: "45Mi"
          requests:
            cpu: "3m"                     # Runs < 2m most of the time
            memory: "25Mi"                # Runs between 25-28Mi
      serviceAccountName: cluster-curator
