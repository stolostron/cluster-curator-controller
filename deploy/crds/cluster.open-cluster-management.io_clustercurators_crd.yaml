apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: clustercurators.cluster.open-cluster-management.io
spec:
  group: cluster.open-cluster-management.io
  names:
    kind: ClusterCurator
    listKind: ClusterCuratorList
    plural: clustercurators
    singular: clustercurator
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterCurator is the Custom Resource object which holds the
          desired state and current status of an cluster. ClusterCurator is a namespace
          scoped resource
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: spec holds desired configuration to create the job
            properties:
              action:
                description: 'Desired action: create/import/destroy/detach/noop action
                  holds the information about create/import/destroy/detach/noop one
                  of the action job should perform'
                type: string
              job:
                description: Jobs holds the job specifications
                items:
                  description: JobSpec holds desired configuration to create the job
                  properties:
                    image:
                      description: image represents image which will be used to run
                        a job OPTIONAL required if NOT ansible
                      type: string
                    name:
                      description: name represents the name of the job
                      type: string
                    runOnAction:
                      description: RunOnAction represents on which actoin this job
                        should be executed action are create/import-only/destroy/detach
                      type: string
                    type:
                      description: type represents type of job
                      type: string
                    values:
                      additionalProperties:
                        type: object
                      description: Values represents neccasary fields required to
                        exceute the job
                      type: object
                  type: object
                type: array
              postJobs:
                description: PostJobs holds the job specifications
                items:
                  description: JobSpec holds desired configuration to create the job
                  properties:
                    image:
                      description: image represents image which will be used to run
                        a job OPTIONAL required if NOT ansible
                      type: string
                    name:
                      description: name represents the name of the job
                      type: string
                    runOnAction:
                      description: RunOnAction represents on which actoin this job
                        should be executed action are create/import-only/destroy/detach
                      type: string
                    type:
                      description: type represents type of job
                      type: string
                    values:
                      additionalProperties:
                        type: object
                      description: Values represents neccasary fields required to
                        exceute the job
                      type: object
                  type: object
                type: array
              preJobs:
                description: PreJobs holds the job specifications
                items:
                  description: JobSpec holds desired configuration to create the job
                  properties:
                    image:
                      description: image represents image which will be used to run
                        a job OPTIONAL required if NOT ansible
                      type: string
                    name:
                      description: name represents the name of the job
                      type: string
                    runOnAction:
                      description: RunOnAction represents on which actoin this job
                        should be executed action are create/import-only/destroy/detach
                      type: string
                    type:
                      description: type represents type of job
                      type: string
                    values:
                      additionalProperties:
                        type: object
                      description: Values represents neccasary fields required to
                        exceute the job
                      type: object
                  type: object
                type: array
            type: object
          status:
            description: status holds the information about the state of a cluster
              curator resource, managed cluster heealth and cluster health.  It is
              consistent with status information across the Kubernetes ecosystem.
            properties:
              clusterhealth:
                description: ClusterStatus represents state of cluster created using
                  hive
                type: object
              conditions:
                description: conditions describe the state of cluster curator resource
                items:
                  description: Conditions contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    conditions:
                      description: conditions describe the state of job resource
                      properties:
                        lastTransitionTime:
                          description: lastTransitionTime is the last time the condition
                            transitioned from one status to another. This should be
                            when the underlying condition changed.  If that is not
                            known, then using the time when the API field changed
                            is acceptable.
                          format: date-time
                          type: string
                        message:
                          description: message is a human readable message indicating
                            details about the transition. This may be an empty string.
                          maxLength: 32768
                          type: string
                        observedGeneration:
                          description: observedGeneration represents the .metadata.generation
                            that the condition was set based upon. For instance, if
                            .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration
                            is 9, the condition is out of date with respect to the
                            current state of the instance.
                          format: int64
                          minimum: 0
                          type: integer
                        reason:
                          description: reason contains a programmatic identifier indicating
                            the reason for the condition's last transition. Producers
                            of specific condition types may define expected values
                            and meanings for this field, and whether the values are
                            considered a guaranteed API. The value should be a CamelCase
                            string. This field may not be empty.
                          maxLength: 1024
                          minLength: 1
                          pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                          type: string
                        status:
                          description: status of the condition, one of True, False,
                            Unknown.
                          enum:
                          - "True"
                          - "False"
                          - Unknown
                          type: string
                        type:
                          description: type of condition in CamelCase or in foo.example.com/CamelCase.
                            --- Many .condition.type values are consistent across
                            resources like Available, but because arbitrary conditions
                            can be useful (see .node.status.conditions), the ability
                            to deconflict is important. The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                          maxLength: 316
                          pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                          type: string
                      required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                      type: object
                    jobname:
                      description: name represents the name of the job
                      type: string
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
